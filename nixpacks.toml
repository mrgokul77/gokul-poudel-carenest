[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = [
  "python3 -m ensurepip --upgrade",
  "cd backend && python3 -m pip install -r requirements.txt"
]

[start]
cmd = "cd backend && python3 -m gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"